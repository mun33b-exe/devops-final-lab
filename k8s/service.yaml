apiVersion: v1
kind: Service
metadata:
  name: devops-lab-service
spec:
  type: NodePort
  selector:
    app: devops-lab
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
    nodePort: 30080
